
# 向量数据库与嵌入模型详解

## 一、嵌入模型（Embedding Models）

### 1.1 什么是嵌入？

嵌入是将文本（或其他数据）转换为数值向量的过程，这些向量能够捕捉语义信息。相似的文本会有相似的向量表示。

**核心特点**：
- **语义表示**：向量距离反映语义相似度
- **高维空间**：通常768、1024、1536维
- **比较方法**：余弦相似度、点积、欧氏距离

---

### 1.2 常见嵌入模型

#### 开源嵌入模型

| 模型 | 维度 | 特点 |
|------|------|------|
| **Sentence-BERT** | 768 | 经典开源，平衡性能和速度 |
| **all-MiniLM-L6-v2** | 384 | 轻量级，速度快，适合生产 |
| **all-mpnet-base-v2** | 768 | 性能最佳，稍慢 |
| **gte-large** | 1024 | 通用领域强性能 |
| **bge-large-zh-v1.5** | 1024 | 中文性能优秀 |

#### 商业API嵌入模型

| 模型 | 维度 | 提供商 |
|------|------|--------|
| **text-embedding-3-large** | 3072 | OpenAI |
| **text-embedding-3-small** | 1536 | OpenAI |
| **gecko** | 768 | Google Vertex AI |
| **Cohere Embed** | 1024 | Cohere |

---

### 1.3 嵌入模型选择建议

**选择标准**：
1. **性能需求**：MTEB Leaderboard查看模型排名
2. **速度要求**：生产环境通常倾向轻量模型
3. **语言**：有专门的中英文模型
4. **成本**：API vs 自托管权衡

**MTEB (Massive Text Embedding Benchmark)**：权威的嵌入模型评测平台，包含：
- 分类、聚类、配对分类
- 检索、STS（语义相似度）
- 重排序、摘要

---

### 1.4 嵌入最佳实践

**文档预处理**：
- **合理切分**：按段落、章节切分，而非固定字符数
- **保留上下文**：段落间重叠部分，避免语义断裂
- **元数据**：记录来源、章节、时间等信息

**嵌入优化**：
- **批处理**：一次嵌入多个文档，提高效率
- **缓存策略**：相同内容无需重复嵌入
- **版本控制**：模型更新时重新嵌入

---

## 二、向量数据库

### 2.1 什么是向量数据库？

向量数据库是专门设计用来存储、索引和查询高维向量的数据库。

**核心能力**：
- **向量存储**：高效存储大量高维向量
- **相似度搜索**：快速找到最相似的向量
- **索引优化**：ANN（近似最近邻）算法
- **元数据过滤**：结合向量搜索和传统过滤

---

### 2.2 主流向量数据库比较

#### 1. Chroma

**特点**：
- 轻量级，本地运行，零配置
- Python优先，易用性极佳
- 适合开发和原型
- 持久化存储到磁盘

**适用场景**：
- 个人项目、原型开发
- 中小规模应用（&lt;1M向量）
- 需要快速迭代的场景

**基础用法**：
```python
import chromadb

# 初始化客户端
client = chromadb.Client()
collection = client.create_collection("my_collection")

# 添加文档
collection.add(
    documents=["文档1内容", "文档2内容"],
    metadatas=[{"source": "source1"}, {"source": "source2"}],
    ids=["doc1", "doc2"]
)

# 查询
results = collection.query(query_texts=["查询内容"], n_results=2)
```

---

#### 2. Pinecone

**特点**：
- 完全托管的云服务
- 高性能，支持大规模
- 自动扩缩容
- 多种索引算法

**适用场景**：
- 企业级生产环境
- 大规模应用（百万+向量）
- 需要高可用性的场景

**优势**：
- 无需运维
- 全球分布
- 实时更新索引

---

#### 3. Milvus / Zilliz

**特点**：
- 开源企业级向量数据库
- 支持多种索引类型
- 强类型系统
- 丰富的SDK

**适用场景**：
- 需要自托管的企业
- 复杂查询需求
- 混合搜索场景

---

#### 4. Weaviate

**特点**：
- 模块化设计
- 内置向量化
- GraphQL查询
- 知识图谱能力

**适用场景**：
- 语义搜索应用
- 需要知识图谱的场景
- 混合向量+标量查询

---

#### 5. FAISS (Facebook AI Similarity Search)

**特点**：
- 库而非完整数据库
- 极高性能
- 多种索引算法
- CPU/GPU支持

**适用场景**：
- 自建向量检索系统
- 对性能极致要求
- 需要深度定制

---

### 2.3 向量索引算法

#### ANN (Approximate Nearest Neighbor)算法

**常见算法**：
1. **HNSW (Hierarchical Navigable Small Worlds)**：
   - 图结构索引
   - 查询速度快，内存占用较高
   - 大多数向量数据库首选

2. **IVF (Inverted File)**：
   - 倒排文件索引
   - 分桶 + 量化
   - 查询质量/速度可配置

3. **Annoy (Approximate Nearest Neighbors Oh Yeah)**：
   - 树结构
   - 适合静态数据集
   - 内存优化好

---

## 三、RAG中的向量检索

### 3.1 完整RAG流程

```
用户输入
    ↓
查询嵌入 ← (查询向量化)
    ↓
向量检索 ← (与文档向量比较)
    ↓
检索到的文档
    ↓
排序过滤 ← (重排序、元数据过滤)
    ↓
构建提示词 ← (包含检索到的上下文)
    ↓
LLM生成回答
```

---

### 3.2 高级检索技术

#### 1. 混合检索 (Hybrid Search)

**原理**：结合向量搜索和关键词搜索
- 向量搜索：语义理解
- 关键词搜索：精确匹配

**优势**：
- 互补两种搜索方式的缺点
- 提高召回率
- 更稳定的表现

---

#### 2. 重排序 (Reranking)

**原理**：先用向量检索快速找到候选，再用更强的模型重新排序

**常用重排序模型**：
- cross-encoder
- bge-reranker
- colbert

**流程**：
1. 向量检索Top 100
2. 重排序模型重新评估
3. 返回Top 10

---

#### 3. 查询重写 (Query Rewriting)

**问题**：用户查询可能不够好
- 太短或太长
- 表达不清楚
- 多个问题混在一起

**解决方案**：
- **HyDE**：让LLM先生成假设文档，再用这个文档检索
- **多查询**：生成多个相关查询，合并结果
- **子问题**：分解为多个子问题，分别检索

---

#### 4. RAG-Fusion

**原理**：
1. 生成多个不同查询
2. 每个查询独立检索
3. 用RRF (Reciprocal Rank Fusion)合并结果
4. 去重和排序

**优势**：
- 覆盖更多角度
- 减少漏检
- 结果更全面

---

### 3.3 检索评估指标

| 指标 | 说明 |
|------|------|
| **Context Precision** | 检索到的文档是否相关 |
| **Context Recall** | 相关文档是否被检索到 |
| **Faithfulness** | 生成的答案是否基于检索的文档 |
| **Answer Relevance** | 答案是否与问题相关 |

**工具**：
- **Ragas**：综合RAG评估框架
- **DeepEval**：评估库
- **TruLens**：可观测性框架

---

## 四、实用建议

### 4.1 向量数据库选型建议

**小规模 (&lt;100k向量)**：
- 用Chroma，简单够用
- 或FAISS + 自己管理

**中等规模 (100k-1M)**：
- Weaviate、Qdrant
- Chroma也能应付

**大规模 (&gt;1M)**：
- Pinecone、Milvus
- Weaviate集群
- 考虑成本

---

### 4.2 性能优化

**检索速度**：
- 索引优化
- 合适的ef_search参数
- 批量查询

**召回质量**：
- 重排序
- 混合检索
- 查询优化

**存储优化**：
- 向量压缩
- 量化
- 按需加载

---

### 4.3 常见问题及解决方案

**问题1：检索不到相关文档**
- 检查嵌入模型是否合适
- 调整文档切分策略
- 尝试查询重写

**问题2：检索太慢**
- 调整索引参数
- 减少Top K
- 考虑近似搜索

**问题3：答案与检索内容不符**
- 检查提示词设计
- 用重排序提升质量
- 优化元数据过滤

---

## 五、进阶话题

### 5.1 多模态向量

不只是文本，还可以嵌入：
- **图像**：CLIP、SigLIP
- **音频**：Whisper、CLAP
- **代码**：CodeBERT

**应用**：
- 跨模态搜索（文搜图）
- 统一向量空间

---

### 5.2 向量数据库与Graph RAG

**结合知识图谱**：
- 实体和关系嵌入
- 图遍历 + 向量检索
- 更复杂的推理

**工具**：
- Neo4j + 向量索引
- Weaviate (内置图能力)

---

### 5.3 实时更新与增量索引

**挑战**：
- 向量索引重建昂贵
- 需要支持增量更新

**解决方案**：
- 使用支持实时更新的数据库
- 分批更新策略
- 冷热分离架构

---

**向量数据库和嵌入模型是RAG技术的基石，选择合适的组合对系统性能至关重要！**

