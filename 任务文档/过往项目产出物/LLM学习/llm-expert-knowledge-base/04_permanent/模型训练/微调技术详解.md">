
# 大模型微调技术详解

## 一、微调基础

### 1.1 什么是微调？

微调（Fine-tuning）是将预训练模型适应到特定任务或领域的过程。

**与预训练的区别**：
- **预训练**：海量数据，通用能力，计算资源巨大
- **微调**：较小数据，特定能力，计算资源较少

---

### 1.2 为什么需要微调？

**预训练模型的局限**：
- 知识截止日期
- 缺乏特定领域知识
- 可能不符合输出格式要求
- 风格可能不匹配

**微调的好处**：
- 注入新知识
- 对齐特定任务
- 提高回答质量
- 控制输出风格

---

### 1.3 微调类型

| 类型 | 说明 | 适用场景 |
|------|------|----------|
| **全量微调** | 更新所有参数 | 资源充足，追求最佳性能 |
| **LoRA** | 训练低秩适配器 | 资源有限，效率优先 |
| **QLoRA** | LoRA + 4位量化 | 极致资源节约 |
| **Adapter** | 添加适配器层 | 灵活，可复用 |
| **Prefix Tuning** | 优化前缀向量 | 固定基础模型 |

---

## 二、LoRA详解

### 2.1 LoRA原理

**LoRA = Low-Rank Adaptation（低秩适应）**

核心思想：不是更新所有参数，而是添加小的"适配器"矩阵。

**数学表示**：
```
h = Wx + BAx
   \_/   \_/
  原参数  LoRA参数
```

其中：
- B ∈ R^(d×r), A ∈ R^(r×k)
- r 是秩，通常 16-128
- BA 是低秩矩阵

**优势**：
- 参数极少（相比全量微调）
- 不会遗忘原知识
- 可以合并到原模型
- 多个LoRA可以同时存在

---

### 2.2 LoRA关键参数

**秩 (rank, r)**：
- **小r（8-32）**：参数少，效率高，可能性能略低
- **大r（64-128）**：性能好，参数较多
- **推荐**：先从16或32开始

**Alpha (α)**：
- 缩放因子
- 通常设为 r 的 1-2 倍
- lr_scheduler 中控制影响

**目标模块**：
- 通常只修改注意力层
- q_proj, k_proj, v_proj, o_proj
- 有时也修改 MLP 层

**Dropout**：
- LoRA层的dropout
- 帮助泛化
- 0.0-0.1 常见

---

### 2.3 LoRA代码示例 (PEFT)

```python
from peft import LoraConfig, get_peft_model
from transformers import AutoModelForCausalLM

# 加载基础模型
model = AutoModelForCausalLM.from_pretrained("base_model")

# LoRA配置
lora_config = LoraConfig(
    r=16,                          # LoRA秩
    lora_alpha=32,                 # alpha，通常2*r
    target_modules=["q_proj", "v_proj"],  # 目标模块
    lora_dropout=0.05,             # dropout
    bias="none",
    task_type="CAUSAL_LM"
)

# 应用LoRA
model = get_peft_model(model, lora_config)
model.print_trainable_parameters()
# 输出：trainable params: 1,234,567 || all params: 7,000,000,000 || trainable%: 0.0176
```

---

## 三、QLoRA：极致资源节约

### 3.1 QLoRA原理

**QLoRA = Quantized LoRA**

组合技术：
1. **4位量化**：将基础模型量化为4位
2. **LoRA**：在量化基础上训练适配器
3. **NF4 (Normalized Float 4)**：专门的4位数据类型

**效果**：
- 显存需求大幅降低（7B模型可在16GB显卡训练）
- 性能接近全量微调
- 成为最流行的微调方法

---

### 3.2 QLoRA关键技术

**NF4数据类型**：
- 专门为神经网络权重设计的4位浮点格式
- 比普通INT4性能更好
- 保留更多信息

**双量化**：
- 第一量化：权重→NF4
- 第二量化：量化常数→FP8
- 进一步节省显存

**分页优化器**：
- 处理显存不足时的CPU卸载
- 类似操作系统的虚拟内存

---

### 3.3 QLoRA代码示例

```python
from peft import LoraConfig, get_peft_model
from transformers import (
    AutoModelForCausalLM,
    BitsAndBytesConfig,
)
import torch

# 4位量化配置
bnb_config = BitsAndBytesConfig(
    load_in_4bit=True,
    bnb_4bit_use_double_quant=True,
    bnb_4bit_quant_type="nf4",
    bnb_4bit_compute_dtype=torch.bfloat16
)

# 加载量化模型
model = AutoModelForCausalLM.from_pretrained(
    "base_model",
    quantization_config=bnb_config,
    device_map="auto"
)

# LoRA配置同上
lora_config = LoraConfig(...)
model = get_peft_model(model, lora_config)
```

---

## 四、训练流程与最佳实践

### 4.1 数据准备

**数据格式**：
```
Instruction: 给一个描述性标题
Input: 论文摘要
Output: 标题

[示例1]
[示例2]
...
```

**数据质量**：
- 质量 &gt; 数量
- 100-1000高质量样本通常足够
- 去重、去噪、格式统一

**数据增强**：
- 同义改写
- 格式变化
- 温度采样生成

---

### 4.2 训练参数

**学习率 (Learning Rate)**：
- LoRA通常用较大的lr
- 2e-4 到 5e-4 是好的起点
- 用余弦退火或线性衰减

**批量大小 (Batch Size)**：
- 梯度累积来模拟大batch
- 128-1024 有效范围
- 取决于显存

**训练轮数 (Epochs)**：
- 1-5 轮通常足够
- 监控验证损失
- 早停防止过拟合

---

### 4.3 完整训练代码 (TRL)

```python
from trl import SFTTrainer
from datasets import load_dataset

# 加载数据
dataset = load_dataset("your_dataset")

# SFT Trainer
trainer = SFTTrainer(
    model=model,
    train_dataset=dataset["train"],
    args=training_args,
    dataset_text_field="text",
    max_seq_length=512,
)

# 开始训练
trainer.train()

# 保存
trainer.model.save_pretrained("output_dir")
```

---

### 4.4 合并LoRA权重

训练完成后可以合并到原模型：

```python
# 方法1：合并并保存
merged_model = model.merge_and_unload()
merged_model.save_pretrained("merged_model")

# 方法2：推理时动态加载（不合并）
from peft import PeftModel
model = AutoModelForCausalLM.from_pretrained("base_model")
model = PeftModel.from_pretrained(model, "lora_adapter")
```

---

## 五、微调工具与框架

### 5.1 主流框架对比

| 框架 | 特点 | 适用场景 |
|------|------|----------|
| **TRL** | Hugging Face官方，SFT/DPO/PPO | 研究，灵活 |
| **Axolotl** | 配置驱动，多算法支持 | 快速实验 |
| **Unsloth** | 2-5倍速度优化 | 追求效率 |
| **LLaMA-Factory** | 丰富的算法和模型 | 综合需求 |

---

### 5.2 Unsloth：追求极致速度

**优势**：
- 优化的CUDA内核
- 2-5倍速度提升
- 显存使用减少
- 零精度损失

**代码示例**：
```python
from unsloth import FastLanguageModel

# 加载模型
model, tokenizer = FastLanguageModel.from_pretrained(
    "lmsys/mistral-7b",
    load_in_4bit=True
)

# 添加LoRA
model = FastLanguageModel.get_peft_model(
    model,
    r=16,
    target_modules=["q_proj", "k_proj", "v_proj", "o_proj"],
)

# 训练！非常快！
```

---

### 5.3 LLaMA-Factory：一站式解决方案

**支持的算法**：
- SFT（监督微调）
- LoRA、QLoRA
- DPO、IPO、KTO
- ORPO、PPO
- Rejection Sampling

**特点**：
- Web UI
- 丰富的预设
- 支持模型多

---

## 六、微调评估与测试

### 6.1 常用评估方法

**自动Benchmark**：
- MMLU：知识测试
- GSM8K：数学推理
- HumanEval：代码生成

**人工评估**：
- 有用性
- 准确性
- 一致性

**基于模型的评估**：
- 用GPT-4当评委
- 评估回复质量

---

### 6.2 常见问题及解决

**问题1：过拟合**
- 现象：训练损失下降，验证损失上升
- 解决：减少epoch，添加dropout，早停

**问题2：灾难性遗忘**
- 现象：微调后忘记原知识
- 解决：用LoRA而非全量微调

**问题3：对齐不足**
- 现象：遵循指令能力差
- 解决：增加高质量的指令数据

---

## 七、进阶技巧

### 7.1 混合模型

**思路**：
- 混合多个LoRA
- 不同场景切换
- 动态权重

**实现**：
```python
# 方法1：依次加载切换
model1 = PeftModel.from_pretrained(base, "lora1")
model2 = PeftModel.from_pretrained(base, "lora2")

# 方法2：加权合并
# 一些框架支持混合多个LoRA
```

---

### 7.2 持续学习

**概念**：
- 不断添加新数据
- 增量微调
- 保留旧能力

**方法**：
- 回放旧数据
- 正则化
- LoRA堆叠

---

### 7.3 任务分解

对于复杂任务：
1. 分阶段微调
2. 先学基础技能
3. 再学高级技能
4. 最后整合

---

## 八、硬件与成本

### 8.1 硬件需求预估

| 模型大小 | QLoRA | LoRA | 全量 |
|----------|-------|------|------|
| 7B | 10-16GB | 20-24GB | 48GB+ |
| 13B | 16-24GB | 32-48GB | 80GB+ |
| 70B | 48-80GB | 80-160GB | 多GPU |

---

### 8.2 降低成本的方法

**云服务选择**：
- AutoDL、恒源云等：性价比高
- Lambda Labs、RunPod：按需GPU
- Kaggle、Colab：免费额度

**优化策略**：
- 梯度检查点
- 梯度累积
- 混合精度
- QLoRA

---

**微调是让大模型"为我所用"的关键技术，选择合适的方法可以事半功倍！**

