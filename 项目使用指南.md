# 发动机V2.3 项目使用指南

> 本文档面向 LLM 和人类用户，详细说明项目的工作流和使用方法。

## 1. 核心概念

### 1.1 任务独立工作区
每个任务在 `任务文档/` 下有独立文件夹，互不干扰。新建任务时：
- 从 `任务文档/_template/` 复制模板
- 自动创建任务文件夹结构
- 产出物保存在 `任务文档/{任务}/产出物/`

### 1.2 捡破烂机制
每轮对话自动保存到 `捡破烂/` 目录：
- 原始对话 → `捡破烂/_archive/`
- 每周挖掘 → `捡破烂/_mining-reports/`
- 待审查 → `捡破烂/_buffer/`

### 1.3 认知闭环
双层知识沉淀：
- `memory/` — Markdown 格式，人类可读，7层知识结构
- `.memory/` — SQLite + FTS5 全文检索，机器检索用

### 1.4 不可变产出物
产出物永不修改，只新增版本：
- 历史版本可追溯
- 每个版本独立保存

## 2. 规则体系

规则按优先级排列（编号越小优先级越高）：
1. `rules/00-meta.md` — 元规则
2. `rules/01-llm-behavior.md` — LLM 行为约束
3. `rules/02-project-structure.md` — 项目结构与命名
4. `rules/03-task-execution.md` — 任务执行流程
5. `rules/04-knowledge.md` — 知识管理+捡破烂机制
6. `rules/05-output.md` — 产出物+版本管理
7. `rules/06-hallucination-prevention.md` — 幻觉防控
8. `rules/07-testing.md` — 测试规则
9. `rules/08-external-integrations.md` — 外部系统集成（Plane MCP）

## 3. 使用流程

### 3.1 开始新任务
直接向 LLM 描述你的需求，系统会自动：
1. 在 `任务文档/` 下创建任务文件夹
2. 加载相关规则
3. 推荐合适专家
4. 同步项目状态

### 3.2 查看任务进度
打开 `任务文档/_index.md` 查看所有任务状态。

### 3.3 知识挖掘
每周对 LLM 说"捡破烂"，系统会自动挖掘对话中的隐藏价值。

### 3.4 每日快报上传到 Plane

生成每日快报后，一键上传到 Plane 的"科技快报"模块：

```bash
cd 任务文档/20260731-同行发展情况/产出物/每日快报
python3 upload_to_plane.py --date 2026-08-04 --verify
```

更多 Plane 操作速查表见 `rules/08-external-integrations.md`。

## 4. 项目资产速查

> 新任务直接查这里，不用每次遍历项目。

### 4.1 Plane 关键 ID

| 类型 | 名称 | ID |
|------|------|-----|
| 项目 | 产品交付（SHIP） | `458341b9-94b6-4419-97b9-5d5f8073d6d1` |
| 项目 | 智驭研发（ZHIYU） | `1f03bf83-4999-4299-b8e0-f05ba4cf7465` |
| 模块 | 科技快报（产品交付下） | `1cd55d8a-8c64-4450-bd54-9446ed3f577c` |

> 完整列表见 `rules/08-external-integrations.md`

### 4.2 重要任务位置

| 任务 | 路径 | 说明 |
|------|------|------|
| 同行发展情况 | `任务文档/20260731-同行发展情况/` | 18家AI厂商跟踪+每日快报 |
| 平台功能树 | `任务文档/20260803_平台功能树/` | 智能体平台菜单梳理 |
| Plane集成 | `plane_mcp/` | Plane MCP 客户端 |

## 5. 目录结构

```
.
├── README.md                    # 项目说明
├── 项目使用指南.md               # 本文档
├── .gitignore
├── .bootstrap-complete          # 初始化完成标记
├── 任务文档/                     # 任务工作区
│   ├── _template/               # 任务模板
│   ├── _archive/                # 已归档任务
│   └── _index.md                # 任务索引
├── 捡破烂/                       # 对话存档与挖掘
│   ├── _buffer/                 # 挖掘缓冲区
│   ├── _archive/                # 对话归档
│   └── _mining-reports/         # 挖掘报告
├── rules/                       # 规则体系
├── memory/                      # 知识沉淀
│   ├── sessions/                # 会话归档
│   ├── episodes/                # 事件层
│   ├── facts/                   # 事实层
│   ├── profiles/                # 画像
│   ├── principles/              # 原则层
│   ├── patterns/                # 模式层
│   ├── decisions/               # 决策层
│   └── index/                   # 索引
├── .memory/                     # SQLite 数据库
├── expert-library/              # 专家库
├── agents/                      # 自定义 Agent
├── workflows/                   # 自定义工作流
├── plane_mcp/                   # Plane MCP 客户端
├── logs/                        # 日志
```