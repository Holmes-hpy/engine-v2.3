{
  "name": "delete_project_estimate_point",
  "description": "Delete a single estimate point.\n\nArgs:\n    project_id: UUID of the project\n    estimate_id: UUID of the estimate\n    estimate_point_id: UUID of the estimate point to delete",
  "arguments": {
    "additionalProperties": false,
    "properties": {
      "project_id": {
        "type": "string"
      },
      "estimate_id": {
        "type": "string"
      },
      "estimate_point_id": {
        "type": "string"
      }
    },
    "required": [
      "project_id",
      "estimate_id",
      "estimate_point_id"
    ],
    "type": "object"
  }
}